#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	int lado,area;
	
	printf("insira o tamanho de um dos lados:");
	scanf("%d",&lado);
	 area= lado*lado;
	 
	 printf("a area do quadrado e: %d ",area);
	
	
	
	
	return 0;
}
